const { default: ContentPage } = require("../ContentPage");
import Logo from "../../../public/companies/Vuzix.webp?react"

class VuzixContent extends ContentPage{
    compLogo = Logo;
    content = "Vuzix is a blah blah blah"
}

export default VuzixContent;